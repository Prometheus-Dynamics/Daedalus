# Python: Generated Plugin Fixture

This folder contains a **frozen manifest + runtime** used by Rust examples/tests.

- `manifest.json`: checked-in fixture manifest
- `rt.py`: checked-in runtime module referenced by the manifest

These are not meant to be edited by hand.

