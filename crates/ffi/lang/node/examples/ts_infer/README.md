# Node: TypeScript Inference Demo

Used by `crates/ffi/tests/node_ts_emitter_suite.rs`.

This example is TS-first and uses the repo tooling to:

1) run `tsc` emit
2) infer node defs from decorators/types
3) build a bundle/plugin

See `crates/ffi/tests/node_ts_emitter_suite.rs` for the exact invocation.

