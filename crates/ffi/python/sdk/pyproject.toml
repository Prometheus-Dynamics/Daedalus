[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "daedalus-ffi"
version = "0.1.0"
description = "Python SDK helpers for Daedalus FFI plugins"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["daedalus_ffi*"]
