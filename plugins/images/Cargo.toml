[package]
name = "daedalus-plugins-images"
version.workspace = true
edition.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[features]
default = []
gpu = ["daedalus/gpu"]

[dependencies]
daedalus = { workspace = true, features = ["plugins"] }
image = { workspace = true }
